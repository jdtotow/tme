apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sidecar-prometheus-volume-claim 
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Mi
