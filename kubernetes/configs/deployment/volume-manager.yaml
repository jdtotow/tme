apiVersion: v1
kind: PersistentVolume
metadata:
  name: volume-manager
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/home/manager/volumes"